import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'formularios_1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
