import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'eval2EscuderoAmodeoGuillermoJose',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
