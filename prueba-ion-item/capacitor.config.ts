import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba-ion-item',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
