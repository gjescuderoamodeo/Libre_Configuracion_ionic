import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'examen1GuillermoJoseEscuderoAmodeo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
