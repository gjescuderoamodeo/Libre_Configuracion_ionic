import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'practica27_sidemenu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
